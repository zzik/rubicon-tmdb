import React, { ReactElement } from 'react'

interface Props {
  
}

function SearchBox({}: Props): ReactElement {
  return (
    <div>
      <span>Search</span>
    </div>
  )
}

export default SearchBox
